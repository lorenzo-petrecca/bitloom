# Bitloom

Bitloom è un editor di matrici di pixel progettato per creare caratteri, icone e pattern per display LED o firmware embedded.  
Permette l'export dei dati in **HEX**, **BIN**, o **DEC**, con supporto a:
- ordine bit **LSB/MSB**
- output per **righe** o **colonne**
- modalità **linear** o **block**
- copia rapida negli appunti

L'app è una **PWA**:
- installabile su desktop e mobile
- utilizzabile offline
- aggiornabile tramite banner "Nuova versione disponibile"

---

## 🚀 Demo / Installazione
### Modalità app (consigliata)
1. Apri Bitloom nel browser
2. `Menu → Installa` (oppure usa l'icona di installazione nella barra)
3. Avviala come applicazione (senza barra URL)

---

## 🧩 Sviluppo Locale

```bash
npm install
npm run dev -- --webpack
